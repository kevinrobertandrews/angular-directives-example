import { ExcludeDirective } from './exclude.directive';

describe('ExcludeDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcludeDirective();
    expect(directive).toBeTruthy();
  });
});
