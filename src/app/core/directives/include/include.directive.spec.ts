import { IncludeDirective } from '../include/include.directive';

describe('IncludeDirective', () => {
  it('should create an instance', () => {
    const directive = new IncludeDirective();
    expect(directive).toBeTruthy();
  });
});
